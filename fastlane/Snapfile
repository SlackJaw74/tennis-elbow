# Snapfile for fastlane snapshot
# More info: https://docs.fastlane.tools/actions/snapshot/

# Xcode project path (relative to project root, where fastlane runs from)
project("TennisElbow/TennisElbow.xcodeproj")

# List of devices to generate screenshots for
# Using available simulators that match App Store Connect requirements
devices([
  "iPad Pro 13-inch (M5)",    # 13" Display
  "iPhone 17 Pro Max",        # 6.9" Display (largest iPhone)
  "iPhone 17"                 # 6.3" Display
])

# List of languages to generate screenshots for
languages([
  "en-US"
])

# The name of the scheme which contains the UI Tests
scheme("TennisElbow")

# Only run the screenshot test, not all UI tests
only_testing(["TennisElbowUITests/TennisElbowUITests/testScreenshots"])

# Where should the resulting screenshots be stored?
output_directory("./fastlane/screenshots")

# Clear all previously generated screenshots before creating new ones
clear_previous_screenshots(true)

# Localize simulator to the specified language
localize_simulator(true)

# Remove the '#' to clear all previously generated screenshots before creating new ones
override_status_bar(true)

# Arguments to pass to the app on launch
# launch_arguments(["-skipOnboarding"])

# For more information about all available options run
# fastlane action snapshot
